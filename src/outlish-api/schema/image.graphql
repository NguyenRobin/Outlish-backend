extend type Mutation {
  createImage(input: UploadFile!): Boolean
}

# input structure
type S3Object {
  bucket: String
  key: String
  body: String
}

type S3Payload {
  signedRequest: String
  url: String
}

input UploadFile {
  file: String
}
